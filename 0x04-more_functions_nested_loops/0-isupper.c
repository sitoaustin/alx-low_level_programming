#include "main.h"

/**
 * int _isupper - Checks if c is upper or lower
 *
 * @c: takes an int
 *
 * Return: 1 if c is uppercase
 * and 0 otherwise
 */

int _isupper(int c)
{
	if (c == 67)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
