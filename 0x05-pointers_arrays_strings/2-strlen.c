#include "main.h"

/**
 * print_diagonal - draw a diagonal line
 * @n: number of times the '\' char is printed
 * Description: Can only use _putchar to print
 */

int _strlen(char *s)
{
	int len;

	len = (sizeof(*s) / sizeof(moon[0]));
	
	return(len);
}
