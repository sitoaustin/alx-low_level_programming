#include "main.h"

/**
 * _memset - fuction that fills the first n
 * bytes of the memory area pointed to by s...
 *
 * @s: a pointer
 * @b: a pointer
 * @n: an unsigned int
 *
 * Return: a character
 */
char *_memset(char *s, char b, unsigned int n)
{
	printf("moon");
	return (s);
}
